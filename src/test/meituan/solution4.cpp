#include <algorithm>
#include <iostream>
#include <map>
#include <set>

struct TreeNode {
    int value;
    TreeNode *left, *right;

    TreeNode() : value(0), left(nullptr), right(nullptr) {}

    TreeNode(int val) : value(val), left(nullptr), right(nullptr) {}

    TreeNode(int val, TreeNode *_left, TreeNode *_right) : value(val), left(_left), right(_right) {}
};

int main() {
    int N;
    return 0;
}
